import { Container, Typography } from "@mui/material";

export default function EventDetailPage() {

    return (<>
        <Container sx={{ mt: 4 }}>
            <Typography variant="h5">Event Detail Page</Typography>
        </Container>
    </>);
}