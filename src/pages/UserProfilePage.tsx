import { Container, Typography } from "@mui/material";

export default function UserProfilePage() {
    return (<>
        <Container sx={{ mt: 4 }}>
            <Typography variant="h5">User Profile Page</Typography>
        </Container>
    </>);
}